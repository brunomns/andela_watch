# ============================================================================
# Andela Watch — all dependencies needed to run the system + tests.
# Install:  python -m pip install -r requirements.txt
# Verified against: Python 3.9+
# ============================================================================

# --- API / backend ---------------------------------------------------------
fastapi>=0.110,<1.0       # web framework + auto OpenAPI docs
uvicorn[standard]>=0.27   # ASGI server — run: uvicorn app.main:app (CLI, not imported)
SQLAlchemy>=2.0,<2.1      # ORM over SQLite (swap WATCHDOG_DB_URL for Postgres)
pydantic>=2.5,<3.0        # request/response validation (FastAPI core, pinned explicitly)

# --- Dashboard -------------------------------------------------------------
streamlit>=1.31           # dashboard UI — run: streamlit run dashboard/streamlit_app.py
pandas>=1.4               # dataframes powering the charts/tables
altair>=5.0               # declarative charts (imported directly by the dashboard)

# --- Dev / test ------------------------------------------------------------
pytest>=7.4               # test runner (python -m pytest)
httpx>=0.27               # required by fastapi.testclient.TestClient used in the tests
